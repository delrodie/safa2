{% extends 'base.html.twig' %}

{% block title %}Commune{% endblock %}

{% block body %}
    <h1>Commune</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ commune.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ commune.nom }}</td>
            </tr>
            <tr>
                <th>Slug</th>
                <td>{{ commune.slug }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_backend_commune_index') }}">back to list</a>

    <a href="{{ path('app_backend_commune_edit', {'id': commune.id}) }}">edit</a>

    {{ include('backend_commune/_delete_form.html.twig') }}
{% endblock %}
