<form method="post" action="{{ path('app_backend_finaliste_delete', {'id': fainaliste.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ fainaliste.id) }}">
    <button class="btn">Delete</button>
</form>
