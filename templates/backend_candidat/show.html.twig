{% extends 'base.html.twig' %}

{% block title %}Candidat{% endblock %}

{% block body %}
    <h1>Candidat</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ candidat.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ candidat.nom }}</td>
            </tr>
            <tr>
                <th>Media</th>
                <td>{{ candidat.media }}</td>
            </tr>
            <tr>
                <th>Slug</th>
                <td>{{ candidat.slug }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_backend_candidat_index') }}">back to list</a>

    <a href="{{ path('app_backend_candidat_edit', {'id': candidat.id}) }}">edit</a>

    {{ include('backend_candidat/_delete_form.html.twig') }}
{% endblock %}
