<form method="post" action="{{ path('app_backend_concours_delete', {'id': concour.id}) }}" onsubmit="return confirm('Voulez-vous vraiment supprimer ce concours?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ concour.id) }}">
    <button class="btn btn-danger">Supprimer</button>
</form>
