{{ form_start(form) }}
    <div class="row">
        <div class="col-12 mt-3">{{ form_row(form.nom) }}</div>
        <div class="col-6 mt-3">{{ form_row(form.debut) }}</div>
        <div class="col-6 mt-3">{{ form_row(form.fin) }}</div>
    </div>
    {{ form_widget(form) }}
    <div class="text-end mt-5">
        <a href="{{ path('app_backend_concours_index') }}" class="btn btn-outline-secondary">Annuler</a>
        <button class="btn btn-primary">{{ button_label|default('Enregistrer') }}</button>
    </div>
{{ form_end(form) }}
